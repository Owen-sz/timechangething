#include <stdio.h>
#include <stdlib.h>
#include <sys/time.h>
#include <unistd.h>

#define CLAY_IMPLEMENTATION
#include "clay.h"

int main(int argc, char *argv[]) {
    {
        int time;
        scanf("%d", &time);
        printf("Setting time to %d.", time);

        int date;
    }

    execvp("timedatectl", char *const _Nullable argv[]);
}
// "%d", &userAge);
// printf("You are %d years old.", userAge);
